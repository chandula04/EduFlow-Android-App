<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/profile_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F0F2F5"
    tools:context=".ProfileFragment">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:layout_constraintTop_toTopOf="parent"
        app:navigationIcon="@drawable/abc_ic_ab_back_material"
        app:title="Edit profile" />

    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintBottom_toTopOf="@+id/btnUpdateProfile"
        android:layout_marginBottom="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">

            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/ivProfile"
                android:layout_width="100dp"
                android:layout_height="100dp"
                android:layout_gravity="center_horizontal"
                android:scaleType="centerCrop"
                app:shapeAppearanceOverlay="@style/ShapeAppearance.Material3.Corner.Full"
                tools:src="@drawable/ic_profile_user" />

            <TextView
                android:id="@+id/tvNameDisplay"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="16dp"
                android:textColor="@android:color/black"
                android:textSize="22sp"
                android:textStyle="bold"
                tools:text="Your Name"/>

            <TextView
                android:id="@+id/tvCustomId"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:textSize="16sp"
                tools:text="\@12345678" />

            <TextView style="@style/ProfileLabel" android:text="Name" />
            <EditText android:id="@+id/etName" style="@style/ProfileEditText" android:hint="Your full name"/>

            <TextView style="@style/ProfileLabel" android:text="Email"/>
            <EditText android:id="@+id/etEmail" style="@style/ProfileEditText" android:hint="your.email@example.com" android:enabled="false"/>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnChangeEmail"
                style="@style/Widget.Material3.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Request Email Change"
                android:paddingStart="0dp"
                android:paddingEnd="0dp"/>

            <TextView style="@style/ProfileLabel" android:text="Birthday"/>
            <EditText android:id="@+id/etBirthdate" style="@style/ProfileEditText" android:hint="DD / MM / YYYY" android:drawableEnd="@drawable/ic_calendar" android:focusable="false"/>

            <TextView style="@style/ProfileLabel" android:text="Gender"/>
            <Spinner android:id="@+id/spinnerGender" style="@style/ProfileEditText" android:enabled="false"/>

            <TextView style="@style/ProfileLabel" android:text="Phone Number"/>
            <EditText android:id="@+id/etPhone" style="@style/ProfileEditText" android:hint="Your phone number" android:inputType="phone"/>

            <TextView style="@style/ProfileLabel" android:text="School"/>
            <EditText android:id="@+id/etSchool" style="@style/ProfileEditText" android:hint="Your school name" android:inputType="textCapWords"/>

            <TextView style="@style/ProfileLabel" android:text="Grade"/>
            <EditText android:id="@+id/etGrade" style="@style/ProfileEditText" android:hint="Your Grade (1-12)" android:inputType="number"/>

            <TextView style="@style/ProfileLabel" android:text="Bio"/>
            <EditText android:id="@+id/etBio" style="@style/ProfileEditText" android:hint="Tell us about yourself..." android:minLines="3" android:gravity="top"/>

        </LinearLayout>
    </ScrollView>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnUpdateProfile"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="8dp"
        android:backgroundTint="@android:color/black"
        android:paddingVertical="14dp"
        android:text="Edit Profile"
        app:cornerRadius="28dp"
        app:layout_constraintBottom_toTopOf="@+id/btnDeleteAccount"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnDeleteAccount"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Delete Account"
        android:textColor="#F44336"
        android:layout_marginBottom="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>